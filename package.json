{
  "name": "cosmeticoskoreanos",
  "version": "1.0.0",
  "scripts": {
    "build": "hugo --minify",
    "og:generate": "node scripts/generate-og-playwright.js"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}
